{
  "name": "xchtip-shortener",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "xchtip.app URL shortener Lambda — mints + resolves short *.xchtip.app links to deterministic /jar tip pages.",
  "main": "index.mjs",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  }
}
